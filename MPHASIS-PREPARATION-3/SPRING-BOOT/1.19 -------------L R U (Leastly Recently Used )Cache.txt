## LRU Cache (Least Recently Used Cache)

**LRU Cache** is a caching strategy that removes the **least recently used item** when the cache reaches its capacity.

No. **`LinkedHashMap` is not an LRU Cache by default.**

By default, `LinkedHashMap` maintains **insertion order**, not **access order**.

### Default Behavior

```java
Map<Integer, String> map = new LinkedHashMap<>();

map.put(1, "A");
map.put(2, "B");
map.put(3, "C");

map.get(1);

System.out.println(map);
```

Output:

```text
{1=A, 2=B, 3=C}
```

Even after accessing key `1`, the order doesn't change.

***

### To Make It LRU-like

Use the constructor with `accessOrder = true`:

```java
LinkedHashMap<Integer, String> map =
        new LinkedHashMap<>(16, 0.75f, true);

map.put(1, "A");
map.put(2, "B");
map.put(3, "C");

map.get(1);

System.out.println(map);
```

Output:

```text
{2=B, 3=C, 1=A}
```

Key `1` moves to the end because it was recently accessed.

***

### Complete LRU Cache

```java
class LRUCache<K, V> extends LinkedHashMap<K, V> {

    private final int capacity;

    public LRUCache(int capacity) {
        super(capacity, 0.75f, true); // access order
        this.capacity = capacity;
    }

    @Override
    protected boolean removeEldestEntry(Map.Entry<K,V> eldest) {
        return size() > capacity;
    }
}
```

Usage:

```java
LRUCache<Integer, String> cache = new LRUCache<>(3);

cache.put(1, "A");
cache.put(2, "B");
cache.put(3, "C");

cache.get(1);     // 1 becomes MRU
cache.put(4, "D"); // removes 2

System.out.println(cache);
```

Output:

```text
{3=C, 1=A, 4=D}
```

### Interview Answer

> `LinkedHashMap` is **not an LRU cache by default** because it maintains insertion order. To implement LRU behavior, create it with `accessOrder=true` and override `removeEldestEntry()`. This makes recently accessed entries move to the end and automatically evicts the least recently used entry when capacity is exceeded.


### Example

Cache Capacity = 3

```text
Put(1,A) -> [1]
Put(2,B) -> [1,2]
Put(3,C) -> [1,2,3]

Get(1)   -> [2,3,1]  // 1 becomes most recently used

Put(4,D)
```

Cache is full, so remove the **least recently used** key, which is **2**.

```text
[3,1,4]
```

***

## Real-World Uses

	* Browser cache
	* Database query cache
	* Redis cache
	* Operating system page replacement
	* API response caching

***

## Interview Requirement

Operations should be:

```text
get(key)  -> O(1)
put(key,value) -> O(1)
```

Using only a HashMap is not enough because finding the least recently used element takes O(n).

### Optimal Data Structure

Combine:

```text
HashMap + Doubly Linked List
```

#### HashMap

Stores:

```text
key -> Node
```

for O(1) access.

#### Doubly Linked List

Maintains usage order.

```text
Head <-> Most Recently Used

Tail <-> Least Recently Used
```

***

## Visualization

Capacity = 3

```text
Put(1)
Head <-> 1 <-> Tail

Put(2)
Head <-> 2 <-> 1 <-> Tail

Put(3)
Head <-> 3 <-> 2 <-> 1 <-> Tail
```

Access key 1:

```text
Get(1)

Head <-> 1 <-> 3 <-> 2 <-> Tail
```

Add key 4:

```text
Put(4)

Remove 2 (LRU)

Head <-> 4 <-> 1 <-> 3 <-> Tail
```

***

## Java Implementation (Interview Standard)

```java
import java.util.HashMap;
import java.util.Map;

public class LRUCache {

    class Node {
        int key;
        int value;
        Node prev;
        Node next;

        Node(int key, int value) {
            this.key = key;
            this.value = value;
        }
    }

    private final int capacity;
    private final Map<Integer, Node> cache;
    private final Node head;
    private final Node tail;

    public LRUCache(int capacity) {
        this.capacity = capacity;
        this.cache = new HashMap<>();

        head = new Node(0, 0);
        tail = new Node(0, 0);

        head.next = tail;
        tail.prev = head;
    }

    public int get(int key) {

        if (!cache.containsKey(key)) {
            return -1;
        }

        Node node = cache.get(key);

        remove(node);
        insert(node);

        return node.value;
    }

    public void put(int key, int value) {

        if (cache.containsKey(key)) {
            remove(cache.get(key));
        }

        Node node = new Node(key, value);

        cache.put(key, node);
        insert(node);

        if (cache.size() > capacity) {

            Node lru = tail.prev;

            remove(lru);
            cache.remove(lru.key);
        }
    }

    private void insert(Node node) {

        Node nextNode = head.next;

        head.next = node;
        node.prev = head;

        node.next = nextNode;
        nextNode.prev = node;
    }

    private void remove(Node node) {

        node.prev.next = node.next;
        node.next.prev = node.prev;
    }
}
```

***

## Simpler Java Solution Using `LinkedHashMap`

Java provides a built-in LRU implementation using `LinkedHashMap`.

```java
import java.util.LinkedHashMap;
import java.util.Map;

public class LRUCache<K, V> extends LinkedHashMap<K, V> {

    private final int capacity;

    public LRUCache(int capacity) {
        super(capacity, 0.75f, true);
        this.capacity = capacity;
    }

    @Override
    protected boolean removeEldestEntry(
            Map.Entry<K, V> eldest) {
        return size() > capacity;
    }
}
```

Usage:

```java
LRUCache<Integer, String> cache =
        new LRUCache<>(3);

cache.put(1, "A");
cache.put(2, "B");
cache.put(3, "C");

cache.get(1);

cache.put(4, "D");

System.out.println(cache);
```

Output:

```text
{3=C, 1=A, 4=D}
```

***

## Interview Answer (2-Minute Version)

> LRU (Least Recently Used) Cache is a cache eviction policy where the item that has not been accessed for the longest time 
is removed when the cache is full. 

To achieve O(1) `get()` and `put()` operations, we use a combination of a HashMap and a
 Doubly Linked List. The HashMap provides fast lookups, while the Doubly Linked List maintains the access order, 
 keeping the most recently used item at the head and the least recently used item at the tail. Java's `LinkedHashMap` 
 can also be configured to implement an LRU Cache efficiently.
