

SQL-QUERIES

1) How handle failure scenario in DB

2) Difference between sql vs no sql and when will used which DB

3) N th highest salary using the same “correlated subquery” pattern

	-- :n is the rank you want (e.g. 2 → 2nd‑highest, 5 → 5th‑highest)
	SELECT DISTINCT salary
	FROM   employees e1
	WHERE  :n = (
			SELECT COUNT (DISTINCT salary)          -- how many DISTINCT salaries
			FROM   employees e2
			WHERE  e2.salary >= e1.salary           -- that are ≥ this salary
	);
	
4) N‑th lowest salary (just flip the comparison)

	SELECT DISTINCT salary
	FROM   employees e1
	WHERE  :n = (
			SELECT COUNT (DISTINCT salary)
			FROM   employees e2
			WHERE  e2.salary <= e1.salary            -- ≤ for ascending order
	);
	
5) Max and Min salary per department

	SELECT dept_id, MAX(salary) AS max_salary, MIN(salary) AS min_salary
	FROM Employee
	GROUP BY dept_id;
	
	Departments having more than 3 employees

	SELECT dept_id
	FROM Employee
	GROUP BY dept_id
	HAVING COUNT(*) > 3;
	
6) Find the second highest salary

	SELECT MAX(salary) AS second_highest
	FROM Employee
	WHERE salary < (
		SELECT MAX(salary) FROM Employee
	);
	
7) Average salary per department

	SELECT dept_id, AVG(salary) AS avg_salary
	FROM Employee
	GROUP BY dept_id
	
8) Count employees in each department:

	SELECT dept_id, COUNT(*) AS emp_count
	FROM Employee
	GROUP BY dept_id;	
	
9) write SQL query: Product, Customer and Order tables 

I want product name laptop and order date less than 30 days

10) 		Dept             
			Dept id            
			Dept name
			 
			Employee
			Emp id,
			Emp name
			Salary
			Dept id
			
			select dept_id ,max(e.salary)  from emp e
			join dept d on 
			e.dept_id = d.dept_id
			group by d.dept_id
			
11) Find highest salary from each deperatment

12) sql max salary of each department

13) Index in database

14) Types of index
 















			
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	





































